
import React, {useState} from 'react'
import Data from '../DataFile/Data.json'

export default function Read()
{
    const [value, setValue] = useState(Data)
    const indexArray = Data.map((item, index) =>{
        return {
         "ProdId":item.id,
         "SizeIndex":0
        }
    })
    console.log(indexArray)
    
    const selectChange = e => {

      alert(e.target.value)
    }
 const body =   Data.map((item, index) => {
     return (
      <div>

      <div className="card">
         <div className="card-header">
             {item.Name}
          </div>
          <div className="card-body">
          <select onChange={selectChange}>
          {item.Sizes.map((item, index) => {
             
              return (
                  
              <option value={index+1 +"-"+item.id}>{item.Size}</option>
              )
             
            })
          }
      </select>
          </div>
          <div className="card-footer">
            {item.Sizes[indexArray[item.id].SizeIndex].Quantity}
          </div>
      </div>
      </div>
    )});

    


return(

<div>
    {body}
    
</div>
)

}

---------------------------------------------------

Data[
    {
        "id":0,
        "Name":"LED Lights",
        "Sizes": [
            {
                "id":1,
                "Quantity":50,
                "Size":"L",
                "Cost":650
            },
            {
                "id":2,
                "Quantity":40,
                "Size":"M",
                "Cost":550   
            }
        ]
    },
    
    {
        "id":1,
        "Name":"Borewell Motors",
        "Sizes": [
            {    "id":0,
                "Quantity":10,
                "Size":"L",
                "Cost":8500
            },
            {   
                "id":1,
                "Quantity":25,
                "Size":"S",
                "Cost":6500  
            }
        ]
    }


]
